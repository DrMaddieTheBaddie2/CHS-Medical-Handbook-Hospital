/**
 * CHS Medical Handbook — script.js
 * Entry point. Imports and runs the core app module.
 *
 * Additional feature modules should be added in js/ and
 * referenced here as the project grows.
 */

'use strict';

// Load the core app
(function () {
  const script = document.createElement('script');
  script.src = 'js/app.js';
  document.head.appendChild(script);
})();
